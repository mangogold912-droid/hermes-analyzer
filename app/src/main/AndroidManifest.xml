<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hermes.analyzer">

    <!-- 모든 파일 접근 권한 (Android 11+) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".HermesApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Hermes"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hermes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatGPTActivity"
            android:exported="false"
            android:theme="@style/Theme.Hermes"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".AnalysisResultsActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".TermuxSetupActivity" />
        <activity android:name=".FileBrowserActivity" />
        <activity android:name=".SandboxManagerActivity" />
        <activity android:name=".MemoryManagerActivity" />
        <activity android:name=".AuditLogActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".PluginManagerActivity" />
        <activity android:name=".AgentStatusActivity" />
        <activity android:name=".TerminalActivity" />
        <activity android:name=".BrowserActivity" />

        <activity android:name=".FileManagerActivity" />
        <activity android:name=".GitHubIntegrationActivity" />
        <activity android:name=".IDAMobileActivity" />
        <activity android:name=".EnhancedBrowserActivity" />
        <!-- Termux 연동 서비스 -->
        <service android:name=".termux.TermuxAutomationService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    <activity
      android:name=".AIAgentChatActivity"
      android:windowSoftInputMode="adjustResize"
      android:exported="false" />
  </application>
</manifest>
